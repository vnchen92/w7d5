<h1>Users</h1>

<ul>
<% @users.each do |user| %>

    <li>
        <img src="<%= user.picture_url %>" alt="<%= user.username %>">
        <a href="<%= user_url(@user)  %>"><%= user.username %></a>
    </li>
    <br>
<% end %>
</ul>
